@model AirportSystem.Models.Airport

@{
    ViewBag.Title = "Details";
}


<div>
    <h2>@Html.DisplayFor(m => m.Name)</h2>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
			City
        </dt>
        <dd>
            @Html.DisplayFor(model => model.City.Name)
        </dd>

    </dl>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
<hr />
@Html.Partial("_AddSchedule", new AirportSystem.Models.Schedule())
<div class="row">
	<div class="col-md-6">
		<h4 class="text-center">Arrivels</h4>
		@Html.Partial("_ScheduleList", Model.Arrivel.ToList())
	</div>
	<div class="col-md-6">
		<h4 class="text-center">Depathers</h4>
		@Html.Partial("_ScheduleList", Model.Deparcher.ToList())
	</div>
</div>
